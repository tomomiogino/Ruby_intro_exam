<h1>Ｍｙツイート一覧</h1>
<div class="tweets_index">
  <div class="container">
    <% @tweets.each do |tweet| %>
      <p><%= tweet.content %></p>
    <% end %>
  </div>
</div>
<%= link_to '新しいツイートを投稿する', new_tweet_path %>
